import { OrderItem } from '../types';

/**
 * Default Order List Database
 * All items in the database are populated via Excel bulk file import (.xlsx, .xls).
 */
export const INITIAL_ORDER_LIST: OrderItem[] = [];
